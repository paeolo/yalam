{
  "private": true,
  "workspaces": [
    "packages/*",
    "scripts"
  ],
  "scripts": {
    "test": "cd example && yalam --watch --no-cache .",
    "build": "lerna run build",
    "update-ts-project-refs": "yarn workspace scripts run update-ts-project-refs"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
