{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "cd example && yalam --watch .",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
