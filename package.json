{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "cd example && yalam --watch .",
    "build": "wsrun build"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
